

namespace TestProject.MasterDataFactory.Controllers
{
    public class MachineControllerTest
    {
       
        public void Test()
        {
            /*
            IQueryable<Machine> machines = new List<Machine>
            {
                new Machine(null)
            }.AsQueryable();
            
            var mockSet = new Mock<DbSet<Machine>>();
            mockSet.As<IQueryable<Machine>>().Setup(m => m.Provider).Returns(machines.Provider);
            mockSet.As<IQueryable<Machine>>().Setup(m => m.Expression).Returns(machines.Expression);
            mockSet.As<IQueryable<Machine>>().Setup(m => m.ElementType).Returns(machines.ElementType);
            mockSet.As<IQueryable<Machine>>().Setup(m => m.GetEnumerator()).Returns(machines.GetEnumerator());
            var context = new Mock<Context>();
            context.Setup(c => c.Machines).ReturnDbSet(mockSet.Object);
            var machineController = new MachineController(context.Object);*/
            //var userContextMock = new Mock<Context>();
            
        }
        
    }
}